import machine
import utime
import i2c
import lcd

# Configurar pantalla LCD
i2c = machine.I2C(scl=machine.Pin(22), sda=machine.Pin(21))
lcd = lcd.LCD(i2c, '16x2')

# Mostrar mensaje en la pantalla LCD durante 10 segundos
lcd.clear()
lcd.putstr("Hola, mundo!")
utime.sleep(10)
lcd.clear()
